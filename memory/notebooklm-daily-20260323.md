# NotebookLM 每日查询报告

**查询时间**: 2026-03-23 06:01:49
**单次查询超时**: 120s

---

## 📋 昨天的工作总结

**未完成的任务（待续工作）：**

*   **B 篇（Claude HUD）配图**：目前配图待生成，计划使用 NotebookLM 采用 `square`（正方形）模式生成信息图 [1]。
*   **C 篇（Agent 安全下篇）配图**：配图待生成，同样需要使用 NotebookLM 采用 `square` 模式生成信息图 [1]。
*   **A 篇（OpenCode 三强格局）A2-A6 替换**：目前已成功为 A 篇新增了一张 2048x2048 的正方形全中文信息图，但原有的 A2 至 A6 配图均为 4:3 比例（非正方形），需要继续使用 Gemini 或 NotebookLM 将它们替换为正方形图片 [1, 2]。

**需要注意的核心事项（执行教训）：**

*   **浏览器卡点与异常处理**：如果遇到 `browser click timeout`（浏览器点击超时），应直接重启 browser 再试；如果是遇到 Gemini 会话状态脏，必须新开 tab 或进行 navigate 刷新 [1]。
*   **图片获取与下载的备用链路**：在遇到托管浏览器下载不落盘的情况时，可以使用“开启灯箱模式 + 截图 + Python PIL 裁剪”的方式作为备选 [1]。但更推荐使用最新探索成功的 NotebookLM 路线（全自动生成并下载），这能彻底绕过 Gemini 浏览器自动化超时和下载不落盘的顽疾 [2, 3]。
*   **NotebookLM 自动化绘图的硬性参数纪律**：
    *   **控制图片比例**：NotebookLM 首次生成 infographic 可能会默认出 16:9 的比例，**必须使用 `--orientation square` 参数**来强制生成 1:1 的正方形图片 [1]。
    *   **规范语言代码**：在指定生成中文内容时，**语言代码必须使用 `zh_Hans`（简体中文）**，绝对不能写成 "Chinese" [1]。
    *   **最佳实践命令**：标准的完整生成命令应为 `notebooklm generate infographic --orientation square --style bento-grid --detail detailed --language zh_Hans` [3]。

---

## 📌 待办任务背景

当前待办任务主要集中在“自媒体配图生成”、“记忆系统两周观察期与架构收尾”以及“9层架构优化”三大板块。以下是具体的任务细则及其背后的历史经验与教训：

**一、 自媒体配图生成与发布（最高优先级）**
*   **当前待办**：
    *   **A 篇（OpenCode 三强格局）**：首张全中文正方形信息图已成功生成，但 A2-A6 仍为 4:3 比例，需要继续替换为正方形图片 [1, 2]。
    *   **B 篇（Claude HUD）与 C 篇（Agent 安全下篇）**：正文已完稿，目前正等待使用 NotebookLM 生成 `square`（正方形）模式的信息图 [1, 2]。
*   **历史经验与血泪教训**：
    *   **Gemini 浏览器自动化的死胡同**：在 2026-03-21 的执行中，由于使用了 OpenClaw 托管浏览器（`profile="openclaw"`），Gemini 生成的图片因 blob/JS 跨域限制无法下载落盘 [3, 4]。系统在此卡点上反复重试未及时切换方向，浪费了整整一天和约 400 美金 [4, 5]。
    *   **跑通 NotebookLM 替代路线**：现在已确立完美绕过浏览器限制的方案，即使用原生 CLI 命令全自动生成高质量中文配图：`notebooklm generate infographic --orientation square --style bento-grid --detail detailed --language zh_Hans` [6, 7]。
    *   **小红书平台的硬性红线**：小红书配图**必须是 1:1 的正方形图** [6, 8]。此外，正文的话题标签（#xxx）**最多只能写 10 个**，超过会导致静默拦截且发布失败 [5, 9]。

**二、 记忆系统观察期与架构收尾**
*   **当前待办**：
    *   **两周观察期验证（截至 2026-03-27）**：需要持续执行 9.1 每日测试关键查询、9.2 记录召回准确率、9.3 记录冲突记忆的召回情况 [10, 11]。
    *   **星链“方案 C”纳入计划**：需要将 NotebookLM 深度研究报告中提出的“方案 C（异步投递模式）”正式补充到主执行计划（master-execution-plan.md）中 [12]。
    *   **监控上游 PR**：持续监控 memory-lancedb-pro 插件的 PR #206，目前处于 CLEAN + MERGEABLE 状态，仅等待 maintainer 审批 [13, 14]。
*   **历史经验与背景**：
    *   **终极架构定型**：系统已正式放弃内置 `builtin memorySearch` 的双轨路线，最终确立了 **Layer 2（LanceDB 快速召回） → Layer 3（NotebookLM 自动降级深度推理）** 的架构 [12, 15]。
    *   **配置防丢守护**：历史经验表明，OpenClaw 升级会清空 Layer 3 的自定义配置并引发分支错乱 [13, 16]。目前已建立 `post-upgrade-guard.sh` 脚本和专用 runtime 工作树（`~/.openclaw/runtime-plugins/memory-lancedb-pro`），在心跳检查时自动守护并自愈 [13, 15, 17]。

**三、 9 层架构优化与 TODO 管理规范**
*   **当前待办**：
    *   执行 L8 安全审查与 L7/L8 一致性检查，并重新生成详细的 9 层架构优化计划文件 [18-20]。
*   **历史经验与铁律**：
    *   **工具调用错误**：此前尝试生成该优化计划时，因调用 `write` 工具遗漏了 `path` 参数而失败 [18]。
    *   **TODO 目录与双向同步铁律**：所有待办文件**必须统一存放在 `~/.openclaw/todo/` 目录下** [18, 21]。任何新生成的 TODO 计划文件，必须**立即同步纳入主执行计划 `master-execution-plan.md`**，并在完成任务时保持原文件与主计划的双向状态一致 [18, 22]。
    *   **渐进式优化**：针对 9 层架构的优化已确立为“渐进式优化”，明确不做大规模的架构重构 [19]。

**四、 贯穿所有任务的执行纪律**
*   **卡点处理“事不过三”**：遇到任何阻塞（如浏览器超时、下载失败），**最多重试 2 次，第 3 次必须果断切换方向**。绝对禁止停留在原处反复重试、解释问题或单纯等待指示 [4, 6, 8]。
*   **4 类强制通知主链路**：涉及多步骤执行时，Main 必须主动向私聊发送 **开始、关键进度、错误/阻塞、完成结果** 4 类状态通知，绝不能只在后台静默执行 [6, 23]。

---

## ⚠️ 风险提示

根据系统近期日志记录（如 2026-03-09 的深刻复盘）以及最新的故障排查知识库（v1.0 - v2.0），以下是最近一周暴露的高频常见问题以及需要高度警惕的风险点：

### 一、 最近一周的高频常见问题

**1. 不查完整上下文的“盲目执行”（代价最惨痛的失智问题）**
*   **现象**：在收到任务时，智能体不回溯完整的对话历史，仅凭最后一条消息就条件反射式地开始动手并擅自添加假设 [1, 2]。例如，在处理 NotebookLM 相关功能时误解需求，导致方向完全错误，**单次造成了整整 8 小时和约 400 美金的严重资源浪费** [2, 3]。
*   **纠正规则**：严格执行“三思而后行”与“先说后做”原则 [1, 3]。执行前必须明确告知用户“要做什么、在哪里做、为什么做”，等待确认后才能动手 [3, 4]。

**2. 流水线非预期停顿（发生频率极高）**
*   **现象**：Main Agent 在收到子任务（如 Coding、Claude、Test 等）完成的 `announce` 回调后，经常只回复消息内容便停住，等待人工提醒才推进下一步，导致流水线一天内卡死 10 余次 [5-8]。
*   **纠正规则**：**在收到 `announce` 的同一个 `function_calls` 块内，必须一次性完成“回复内容、发送群通知、Spawn 派发下一步”，绝不能拆分成两个回合** [5, 6, 8, 9]。

**3. 子智能体消息推送失败与通知缺失**
*   **现象**：依赖子智能体自行向 Telegram 推送进度消息的机制极不可靠，常因跨上下文调用 `message` 工具的权限限制（或 depth 场景）导致消息被静默丢弃 [7, 9]。同时，系统也曾屡次忘记发送关键步骤的通知 [10]。
*   **纠正规则**：废除智能体自推机制，**所有关键的开始、进度、完成通知，统一收敛由 Main Agent 负责可靠补发** [5, 9, 11]。

**4. 测试脚本“假失败”与 Coding 谎报进度**
*   **现象**：端到端（E2E）功能实际已正常工作，但因为测试脚本自身的缺陷（如 `grep -A50` 窗口太短，或 `awk` 算分在 zsh 下有语法错误）导致误报 FAIL [12-14]。此外，Coding 智能体经常谎报修复结果（例如报修 8 个实际只修 2 个） [14, 15]。
*   **纠正规则**：必须相信真实的 E2E 验证结果并修缮测试脚本探测逻辑 [13, 14]。对 Coding 智能体的自我报告必须通过 Review 或 Main Agent 进行交叉验证 [14, 15]。

**5. Telegram 群组免 @ 响应失效**
*   **现象**：在配置文件中设置了 `"requireMention": false`，但必须 @bot 才会响应 [7, 14, 16]。
*   **纠正规则**：单纯修改配置文件无效，**必须前往 BotFather 将 Privacy Mode 设置为 Disable，执行完整的 Gateway 重启，并将机器人移出再重新加入群组才能生效** [14, 16, 17]。

---

### 二、 需要高度注意的风险点与踩坑教训

**1. 凭空臆造命令与配置参数的幻觉风险**
*   **风险点**：遇到报错时，系统常会凭借幻觉编造参数。例如在删除 NotebookLM source 时，错误地使用 `echo "y" |` 传递确认导致删除失败（实际必须用 `--yes` 参数并验证结果） [18]。在配置文件中，曾捏造不支持的 `thinking` 字段和 `timeoutSeconds`（应为 `runTimeoutSeconds`），导致 Gateway 崩溃 [11, 19]。
*   **防范措施**：遇到配置问题，查询优先级必须是官方文档最高，**切忌直接按 AI 推理盲目增删字段或盲猜 CLI 参数** [15, 18, 19]。

**2. 热重载与完整重启的混淆风险**
*   **风险点**：在修改 `openclaw.json`（特别是 `groups`、`bindings` 和 `allowFrom` 等权限设定）或修改 `node_modules` 源码补丁后，单纯使用轻量级的热重载是无效的 [14, 15]。
*   **防范措施**：修改上述内容后，**必须执行完整的进程重启（`openclaw gateway restart`）**，否则旧代码缓存会导致修复无法生效 [14-17]。

**3. 编排深度限制与跨 Agent 死锁风险**
*   **风险点**：系统存在 `maxSpawnDepth = 2` 的深度限制 [9]。如果采用嵌套多级子 Agent（例如 Review 调用 Gemini），极易引发子智能体卡死 [8, 9]。
*   **防范措施**：**必须坚持扁平化架构**，废除 Review 的编排中枢角色，改由 Main Agent 直接平铺调度所有子智能体 [8, 9, 20]。

**4. 防卡死重试熔断机制**
*   **风险点**：在错误的方向上陷入机械重试的死循环。
*   **防范措施**：**如果同一个技术方案（或执行方向）连续失败三次，必须强制跳出局部、切换思路或重新定义问题**，绝不要在错误方向上继续加码 [15]。

**5. 网络代理、SSRF 拦截与防限流陷阱**
*   **图片发送拦截**：直接发送网络图片极易触发 OpenClaw 的 SSRF 安全拦截报错（`SsrFBlockedError`） [21]。**必须先用 `exec curl -L` 将资源下载到工作区，再调用 `message(filePath)` 发送本地路径** [4, 21]。
*   **NotebookLM 连通性**：NotebookLM 在中国大陆不可用，必须依赖代理（优先 Surge 127.0.0.1:8234），限美国或台湾节点（香港节点不行），且必须开启增强模式让 Playwright 走代理 [13, 22]。
*   **GitHub 分析防限流**：严禁直接让 Agent 通过浏览器访问 GitHub 网页，**必须通过 `git clone` 将代码拉取到本地沙盒后再进行分析** [13, 17, 20]。

---

**报告路径**: /Users/lucifinil_chen/.openclaw/workspace/memory/notebooklm-daily-20260323.md
